sensor:
  - platform: wifi_signal
    name: WiFi Signal ${short_name} 
  - platform: uptime
    name: Uptime ${short_name} 
#  - platform: internal_temperature
#    name: "Internal Temperature"

  - platform: dallas
#    address: 0xc600000ca617a628
    index: 0
    name: "Temperature ${short_name}"
    id: tepm_pool
    icon: "mdi:water-percent"
    device_class: "temperature"
    state_class: "measurement"
    accuracy_decimals: 1
    on_value_range:
      - above: !lambda 'return id(set_temp_pool).state;'
        then:
          - switch.turn_off: ${name}_switch2
      - below: !lambda 'return id(set_temp_pool).state;'
        then:
          if:
            condition:
              - switch.is_on: ${name}_switch1
            then: 
              - switch.turn_on: ${name}_switch2
      # - above: 5.0
        # below: 10.0
        # then:
          # - switch.turn_on: ${name}_switch_2
